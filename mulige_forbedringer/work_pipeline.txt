# Двухнедельный
# Понедельник. Приходят новые креативы. Записываются в бд. Там они с утра выгружаются и разделяются на два датасета: с пустой категорией
# и с проставленной. С проставленной отдаются оператору, он их перепроставляет и они обновляются в бд и отдаются в обучающий датасет.
# Модель на нём заново обучается. И делает предсказания для датасета с пустой категорией. Оператор их перепроверяет.
# И они записываются в бд тоже. И в обучающий датасет для следующего дня уже.
# Вторник. Приходят новые креативы. Записываются в бд. Там они с утра выгружаются и разделяются на два датасета: с пустой категорией
# и с проставленной. С проставленной отдаются оператору, он их перепроставляет и они обновляются в бд и отдаются в обучающий датасет.
# Модель на нём заново обучается. И делает предсказания для датасета с пустой категорией. Оператор их уже НЕ перепроверяет.
# И они записываются в бд тоже. Но в обучающий датасет НЕ записываются.
# Среда. Приходят новые креативы. Записываются в бд. Там они с утра выгружаются и разделяются на два датасета: с пустой категорией
# и с проставленной. С проставленной отдаются модели, она им делает предсказания. Совпадающие значения сразу записываются.
# А несовпадающие рандомным образом либо остаются прежними либо перезаписываются предсказаниями модели.
# Этот датасет обновляется в бд, а также добавляется в обучающий для следующего дня.
# Модель делает предсказания для датасета с пустой категорией. И они записываются в бд тоже. Но в обучающий датасет НЕ записываются.
# Четверг. Приходят новые креативы. Записываются в бд. Там они с утра выгружаются и разделяются на два датасета: с пустой категорией
# и с проставленной. С проставленной отдаются оператору, он их перепроставляет и они обновляются в бд и отдаются в обучающий датасет.
# Модель на нём заново обучается. И делает предсказания для датасета с пустой категорией. Оператор их перепроверяет.
# И они записываются в бд тоже. И в обучающий датасет для следующего дня уже.
# Пятница. Приходят новые креативы. Записываются в бд. Там они с утра выгружаются и разделяются на два датасета: с пустой категорией
# и с проставленной. С проставленной отдаются модели, она им делает предсказания. Совпадающие значения сразу записываются.
# А несовпадающие рандомным образом либо остаются прежними либо перезаписываются предсказаниями модели.
# Этот датасет обновляется в бд, а также добавляется в обучающий для следующего дня.
# Модель делает предсказания для датасета с пустой категорией. И они записываются в бд тоже. Но в обучающий датасет НЕ записываются.
# 
# Понедельник. Приходят новые креативы. Записываются в бд. Там они с утра выгружаются и разделяются на два датасета: с пустой категорией
# и с проставленной. С проставленной отдаются модели, она им делает предсказания. Совпадающие значения сразу записываются.
# А несовпадающие рандомным образом либо остаются прежними либо перезаписываются предсказаниями модели.
# Этот датасет обновляется в бд, а также добавляется в обучающий для следующего дня.
# Модель делает предсказания для датасета с пустой категорией. И они записываются в бд тоже. Но в обучающий датасет НЕ записываются.
# Вторник. Приходят новые креативы. Записываются в бд. Там они с утра выгружаются и разделяются на два датасета: с пустой категорией
# и с проставленной. С проставленной отдаются оператору, он их перепроставляет и они обновляются в бд и отдаются в обучающий датасет.
# Модель на нём заново обучается. И делает предсказания для датасета с пустой категорией. Оператор их перепроверяет.
# И они записываются в бд тоже. И в обучающий датасет для следующего дня уже.
# Среда. Приходят новые креативы. Записываются в бд. Там они с утра выгружаются и разделяются на два датасета: с пустой категорией
# и с проставленной. С проставленной отдаются модели, она им делает предсказания. Совпадающие значения сразу записываются.
# А несовпадающие отдаются на проверку оператору.
# Этот датасет обновляется в бд, а также добавляется в обучающий для следующего дня.
# Модель делает предсказания для датасета с пустой категорией. И они записываются в бд тоже. Но в обучающий датасет НЕ записываются.
# Четверг. Приходят новые креативы. Записываются в бд. Там они с утра выгружаются и разделяются на два датасета: с пустой категорией
# и с проставленной. С проставленной отдаются оператору, он их перепроставляет и они обновляются в бд и отдаются в обучающий датасет.
# Модель на нём заново обучается. И делает предсказания для датасета с пустой категорией. Оператор их перепроверяет.
# И они записываются в бд тоже. И в обучающий датасет для следующего дня уже.
# Пятница. Приходят новые креативы. Записываются в бд. Там они с утра выгружаются и разделяются на два датасета: с пустой категорией
# и с проставленной. С проставленной отдаются оператору, он их перепроставляет и они обновляются в бд и отдаются в обучающий датасет.
# Модель на нём заново обучается. И делает предсказания для датасета с пустой категорией. Оператор их уже НЕ перепроверяет.
# И они записываются в бд тоже. Но в обучающий датасет НЕ записываются.

# Для проекта проще пайплайн:
# С утра обновляется обучающий датасет, модель обучается на нём.
# Потом делает предсказания на рабочем датасете с пустой категорией и записывает
# предсказанный вариант датасета.
# Как в свою очередь модель заново обучается в питон модулях:
# Берёт вчерашнее значение лучших параметров. Замеряет на нём метрику. Обучается
# заново на обновлённых данных. Если метрика больше -- берёт новые параметры,
# иначе оставляет вчерашние. Процент несовпадающих значений так же выводится на
# фронте как метрика.
# check_overfitting должен проверяться автоматически, или убраться. Если
# переобучение -- тренировка модели происходит заново.
